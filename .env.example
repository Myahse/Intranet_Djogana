# Frontend (optional): leave empty in dev to use Vite proxy to backend at http://localhost:3000
# VITE_API_BASE_URL=
# Or set explicitly: VITE_API_BASE_URL=http://localhost:3000

# Backend (server/.env or root .env) — required for npm run server
# JWT_SECRET=your-secret-at-least-32-chars  (use a strong random value in production)
# PORT=3000
# PUBLIC_BASE_URL=http://localhost:3000
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
#   If you see a libpq SSL warning, add: ?sslmode=verify-full or &uselibpqcompat=true to the query string.

# Cloudinary (file storage — replaces storing files as bytea in PostgreSQL)
# Get these from your Cloudinary dashboard: https://console.cloudinary.com/settings/api-keys
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=123456789012345
# CLOUDINARY_API_SECRET=your-api-secret

# Firebase Admin SDK (for push notifications via FCM)
# Option A: Set the path to your service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json
# Option B: Set the JSON content directly (useful for Render, Railway, Heroku, etc.)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
#   Get the service account JSON from Firebase Console:
#   Project Settings → Service Accounts → Generate New Private Key

# Run backend: node server/index.cjs  (default port 3000; set PORT if 3000 is in use)
# Run frontend: npm run dev           (port 5173, proxies /api and /files to 3000)
